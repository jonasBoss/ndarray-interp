mod interp1d;
mod interp2d;
mod vector_extensions;

pub use interp1d::*;
pub use interp2d::*;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
